package euler;

import java.util.ArrayList;

import static math.Properties.isPandigital;
import static misc.Strings.permutations;

public class E43 {

    public static void start() {
        /* long sum = 0;
        String start = "1023456789";
        ArrayList<String> permutations = permutations(start);

        for (String n : permutations) {

            if (isPandigital(n) && Long.parseLong(n.substring(1, 4)) % 2 == 0 &&
                    Long.parseLong(n.substring(2, 5)) % 3 == 0 &&
                    Long.parseLong(n.substring(3, 6)) % 5 == 0 &&
                    Long.parseLong(n.substring(4, 7)) % 7 == 0 &&
                    Long.parseLong(n.substring(5, 8)) % 11 == 0 &&
                    Long.parseLong(n.substring(6, 9)) % 13 == 0 &&
                    Long.parseLong(n.substring(7, 10)) % 17 == 0)
                sum += Long.parseLong(n);
        }

        System.out.println(sum); */
    }
}
