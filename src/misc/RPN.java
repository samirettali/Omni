package misc;

public class RPN {

  /*public static Stack<Double> stack = new Stack<Double>();

  public static double evaluate(String exp) {
    for (int i = 0; i < exp.length(); i++) {
      char x = exp.charAt(i);
      if (x >= '0' && x <= '9')
        stack.push(new Double(x - '0'));

      else if (x == '+') {
        double a = stack.pop();
        double b = stack.pop();
        stack.push(a + b);
      }

      else if (x == '*') {
        double a = stack.pop();
        double b = stack.pop();
        stack.push(a * b);
      }

      else if (x == '/') {
        double a = stack.pop();
        double b = stack.pop();
        stack.push(b / a);
      }

      else if (x == '-') {
        double a = stack.pop();
        double b = stack.pop();
        stack.push(b - a);
      }


    }

    return (double) stack.pop();
  }*/

}